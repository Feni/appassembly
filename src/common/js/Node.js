export class Node {
    // Node structure used for linked-list and tree implementation internally
    constructor(value, left, right) {
        this.value = value;
        this.left = left;
        this.right = right;
    }
}
